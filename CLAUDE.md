# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

**super-train** — a tool to help organize and locate a congregation's field service territories (per `README.md`: "Repositório para facilitar a organização e localização dos territórios de campo da congregação").

## Current State

The repository is currently a skeleton: it contains only `README.md` and an MIT `LICENSE`. There is no source code, build tooling, dependency manifest, or test suite yet.

Once the stack is chosen and code is added, update this file with:
- Build, run, lint, and test commands (including how to run a single test)
- The high-level architecture that spans multiple files
